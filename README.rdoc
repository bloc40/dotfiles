== To Install

  $ git clone git://github.com/bloc40/dotfiles.git
  $ cd dotfiles
  $ ./install

== To remove the symlinks

  $ cd dotfiles
  $ ./remove_symlinks

== To Update

  $ cd dotfiles
  $ git pull --rebase
