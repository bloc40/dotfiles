# ====================
# unix
# ====================
alias v='nvim'
alias mkdir='mkdir -pv'
alias c='clear'
alias h='history'
alias la='ls -lah'
alias las='ls -la | grep "\->"'
alias ll='ls -lh'
alias tarc='tar -czvf'
alias tarx='tar -xzvf'
alias now='date +"%T"'
alias dot='cd ~/Code/dotfiles && v config/nvim/init.vim'
alias noh='privacy_on' # 'set +o history'
alias yesh='privacy_off' # 'set -o history'
alias jsonpp='python3 -m json.tool'
alias ns='npm start'
alias df='df -h'
alias du='du -h'
alias sc='screencapture -i'
alias mute="osascript -e 'set volume output muted true'"
alias unmute="osascript -e 'set volume output muted false'"
alias pubip='curl -s ip.me'
alias isp='curl -s ipinfo.io/org'
alias users="dscl . list /Users | grep -v '^_'"
alias pass="openssl rand -base64 20"

# ====================
# rails
# ====================
alias migrate='bin/rails db:migrate db:rollback && rails db:migrate'
alias rdm='bin/rails db:migrate'
alias rdr='bin/rails db:rollback'
alias rdm0='bin/rails db:migrate VERSION=0'
alias rdms='bin/rails db:migrate db:seed'
alias rs0='be rails s -b 0.0.0.0'
alias edit='EDITOR="nvim" rails credentials:edit'

# ====================
# tmux
# ====================
alias tmux='tmux -f ~/.tmux/tmux.conf'

# ====================
# python
# ====================
alias p3='python3'
